const authRouters = require("./authRouters");
const userRouters = require("./userRouters");
const productRouters = require("./productRouters");

module.exports = {
    authRouters, userRouters, productRouters
}